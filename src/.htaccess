# The classes are loaded by PHP include from index.php, never requested by URL.
# Deny all direct web access to this directory (defense in depth).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
